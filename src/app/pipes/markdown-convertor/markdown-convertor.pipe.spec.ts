import { MarkdownConvertorPipe } from './markdown-convertor.pipe';

describe('MarkdownConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkdownConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
